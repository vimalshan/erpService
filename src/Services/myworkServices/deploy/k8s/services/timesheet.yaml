apiVersion: v1
kind: ConfigMap
metadata:
  name: timesheet-config
  namespace: erp
data:
  ASPNETCORE_ENVIRONMENT: Production
  ASPNETCORE_URLS: http://+:80
  RabbitMQ__HostName: rabbitmq
  JwtSettings__Issuer: TimeSheetService
  JwtSettings__Audience: TimeSheetServiceClients
  AzureBlobStorage__ContainerName: timesheet-attachments
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: timesheet-service
  namespace: erp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: timesheet-service
  template:
    metadata:
      labels:
        app: timesheet-service
    spec:
      containers:
        - name: timesheet-service
          image: erp/timesheet-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: timesheet-config
          env:
            - name: ConnectionStrings__DefaultConnection
              value: "Server=sqlserver;Database=MYWORKDB;User Id=sa;Password=$(DB_SA_PASSWORD);TrustServerCertificate=True;MultipleActiveResultSets=True;Application Name=TimeSheetService"
            - name: DB_SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: db-sa-password
            - name: RabbitMQ__UserName
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: rabbitmq-user
            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: rabbitmq-pass
            - name: JwtSettings__Secret
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: jwt-key-timesheet
            - name: AzureBlobStorage__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: azure-blob-connection-string
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: timesheet-service
  namespace: erp
spec:
  selector:
    app: timesheet-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
