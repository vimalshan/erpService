apiVersion: v1
kind: ConfigMap
metadata:
  name: audit-config
  namespace: erp
data:
  ASPNETCORE_ENVIRONMENT: Production
  ASPNETCORE_URLS: http://+:80
  RabbitMQ__Host: rabbitmq
  RabbitMQ__Port: "5672"
  RabbitMQ__VirtualHost: "/"
  JwtSettings__Issuer: AuditService
  JwtSettings__Audience: AuditServiceClients
  AzureBlobStorage__ContainerName: audit-attachments
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: erp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      containers:
        - name: audit-service
          image: erp/audit-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          envFrom:
            - configMapRef:
                name: audit-config
          env:
            - name: ConnectionStrings__DefaultConnection
              value: "Server=sqlserver;Database=MYWORKDB;User Id=sa;Password=$(DB_SA_PASSWORD);TrustServerCertificate=True;MultipleActiveResultSets=True;Application Name=AuditService"
            - name: DB_SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: db-sa-password
            - name: RabbitMQ__Username
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: rabbitmq-user
            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: rabbitmq-pass
            - name: JwtSettings__SecretKey
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: jwt-key-audit
            - name: AzureBlobStorage__ConnectionString
              valueFrom:
                secretKeyRef:
                  name: erp-secrets
                  key: azure-blob-connection-string
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 80
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: erp
spec:
  selector:
    app: audit-service
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
