namespace AimsTransactionService.Domain.Enums;

/// <summary>Swipe pull/processing status: A = Automatic, M = Manual review.</summary>
public enum PullStatus
{
    Automatic = 'A',
    Manual = 'M'
}
