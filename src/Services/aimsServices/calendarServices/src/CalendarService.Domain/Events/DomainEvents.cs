using CalendarService.Domain.Common;

namespace CalendarService.Domain.Events;

public sealed record CalendarCreatedEvent(int CalendarId, string CalendarName) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredOn { get; } = DateTime.UtcNow;
}

public sealed record CalendarClosedEvent(int CalendarId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredOn { get; } = DateTime.UtcNow;
}

public sealed record HolidayCreatedEvent(int HolidayId, DateTime HolidayDate, string Description) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredOn { get; } = DateTime.UtcNow;
}

public sealed record ShiftCreatedEvent(int ShiftId, string ShiftCode, string ShiftName) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredOn { get; } = DateTime.UtcNow;
}

public sealed record ShiftUpdatedEvent(int ShiftId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredOn { get; } = DateTime.UtcNow;
}
