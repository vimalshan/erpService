apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-provider
  namespace: auth-provider
  labels:
    app: auth-provider
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: auth-provider
  template:
    metadata:
      labels:
        app: auth-provider
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "7136"
    spec:
      serviceAccountName: auth-provider
      securityContext:
        runAsNonRoot: false
        fsGroup: 1000
      containers:
      - name: auth-provider
        image: auth-provider:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 7136
          protocol: TCP
        
        # Environment variables from ConfigMap
        envFrom:
        - configMapRef:
            name: auth-provider-config
        
        # Environment variables from Secret
        env:
        - name: ConnectionStrings__AuthProviderDB
          valueFrom:
            secretKeyRef:
              name: auth-provider-secrets
              key: ConnectionStrings__AuthProviderDB
        - name: Jwt__Secret
          valueFrom:
            secretKeyRef:
              name: auth-provider-secrets
              key: Jwt__Secret
        
        # Resource limits and requests
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        
        # Liveness probe
        livenessProbe:
          httpGet:
            path: /api/v1/minimal/auth/health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        # Readiness probe
        readinessProbe:
          httpGet:
            path: /api/v1/minimal/auth/health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        
        # Startup probe (waits for app to start)
        startupProbe:
          httpGet:
            path: /api/v1/minimal/auth/health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
        
        # Volume mounts
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: logs
          mountPath: /app/logs
        
        # Security context for container
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
          capabilities:
            drop:
            - ALL
      
      # Pod volumes
      volumes:
      - name: config
        configMap:
          name: auth-provider-config
      - name: logs
        emptyDir: {}
      
      # Pod affinity
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - auth-provider
              topologyKey: kubernetes.io/hostname
      
      # Tolerations for node taints
      tolerations:
      - key: "workload"
        operator: "Equal"
        value: "api"
        effect: "NoSchedule"
