apiVersion: apps/v1
kind: Deployment
metadata:
  name: referencedata-service
  namespace: referencedata-service
  labels:
    app: referencedata-service
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: referencedata-service
  template:
    metadata:
      labels:
        app: referencedata-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5195"
    spec:
      serviceAccountName: referencedata-service
      securityContext:
        runAsNonRoot: false
        fsGroup: 1000
      containers:
      - name: referencedata-service
        image: referencedata-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 5195
          protocol: TCP
        envFrom:
        - configMapRef:
            name: referencedata-service-config
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: referencedata-service-secrets
              key: ConnectionStrings__DefaultConnection
        - name: Jwt__Key
          valueFrom:
            secretKeyRef:
              name: referencedata-service-secrets
              key: Jwt__Key
        - name: RabbitMQ__Password
          valueFrom:
            secretKeyRef:
              name: referencedata-service-secrets
              key: RabbitMQ__Password
        - name: AzureBlobStorage__ConnectionString
          valueFrom:
            secretKeyRef:
              name: referencedata-service-secrets
              key: AzureBlobStorage__ConnectionString
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        startupProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: logs
          mountPath: /app/logs
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
      volumes:
      - name: config
        configMap:
          name: referencedata-service-config
      - name: logs
        emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - referencedata-service
              topologyKey: kubernetes.io/hostname
      tolerations:
      - key: "workload"
        operator: "Equal"
        value: "api"
        effect: "NoSchedule"
