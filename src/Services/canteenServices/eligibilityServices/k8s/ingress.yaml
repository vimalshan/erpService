apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: eligibility-service-ingress
  namespace: eligibility-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - eligibility.example.com
    secretName: eligibility-service-tls
  rules:
  - host: eligibility.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: eligibility-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: eligibility-service-network-policy
  namespace: eligibility-service
spec:
  podSelector:
    matchLabels:
      app: eligibility-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5193
  egress:
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5672
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
