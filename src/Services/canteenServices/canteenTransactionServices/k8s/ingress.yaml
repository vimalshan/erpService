apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: canteentransaction-service-ingress
  namespace: canteentransaction-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - canteentransaction.example.com
    secretName: canteentransaction-service-tls
  rules:
  - host: canteentransaction.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: canteentransaction-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: canteentransaction-service-network-policy
  namespace: canteentransaction-service
spec:
  podSelector:
    matchLabels:
      app: canteentransaction-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5195
  egress:
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5672
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
