apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: card-management-service-ingress
  namespace: card-management-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - card-management.example.com
    secretName: card-management-service-tls
  rules:
  - host: card-management.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: card-management-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: card-management-service-network-policy
  namespace: card-management-service
spec:
  podSelector:
    matchLabels:
      app: card-management-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5191
  egress:
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5672
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
