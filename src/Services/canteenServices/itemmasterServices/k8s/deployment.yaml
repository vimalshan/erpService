apiVersion: apps/v1
kind: Deployment
metadata:
  name: itemmaster-service
  namespace: itemmaster-service
  labels:
    app: itemmaster-service
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: itemmaster-service
  template:
    metadata:
      labels:
        app: itemmaster-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5194"
    spec:
      serviceAccountName: itemmaster-service
      securityContext:
        runAsNonRoot: false
        fsGroup: 1000
      containers:
      - name: itemmaster-service
        image: itemmaster-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 5194
          protocol: TCP
        envFrom:
        - configMapRef:
            name: itemmaster-service-config
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: itemmaster-service-secrets
              key: ConnectionStrings__DefaultConnection
        - name: Jwt__Key
          valueFrom:
            secretKeyRef:
              name: itemmaster-service-secrets
              key: Jwt__Key
        - name: RabbitMQ__Password
          valueFrom:
            secretKeyRef:
              name: itemmaster-service-secrets
              key: RabbitMQ__Password
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        startupProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: logs
          mountPath: /app/logs
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
      volumes:
      - name: config
        configMap:
          name: itemmaster-service-config
      - name: logs
        emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - itemmaster-service
              topologyKey: kubernetes.io/hostname
      tolerations:
      - key: "workload"
        operator: "Equal"
        value: "api"
        effect: "NoSchedule"
