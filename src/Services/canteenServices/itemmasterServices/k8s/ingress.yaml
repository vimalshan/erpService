apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: itemmaster-service-ingress
  namespace: itemmaster-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - itemmaster.example.com
    secretName: itemmaster-service-tls
  rules:
  - host: itemmaster.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: itemmaster-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: itemmaster-service-network-policy
  namespace: itemmaster-service
spec:
  podSelector:
    matchLabels:
      app: itemmaster-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5194
  egress:
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5672
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
