apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: canteen-unit-service-ingress
  namespace: canteen-unit-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - canteen-unit.example.com
    secretName: canteen-unit-service-tls
  rules:
  - host: canteen-unit.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: canteen-unit-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: canteen-unit-service-network-policy
  namespace: canteen-unit-service
spec:
  podSelector:
    matchLabels:
      app: canteen-unit-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5190
  egress:
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5672
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
