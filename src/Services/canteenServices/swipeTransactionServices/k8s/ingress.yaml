apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swipe-transaction-service-ingress
  namespace: swipe-transaction-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - swipe-transaction.example.com
    secretName: swipe-transaction-service-tls
  rules:
  - host: swipe-transaction.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: swipe-transaction-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: swipe-transaction-service-network-policy
  namespace: swipe-transaction-service
spec:
  podSelector:
    matchLabels:
      app: swipe-transaction-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5196
  egress:
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 5672
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
