apiVersion: apps/v1
kind: Deployment
metadata:
  name: swipe-transaction-service
  namespace: swipe-transaction-service
  labels:
    app: swipe-transaction-service
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: swipe-transaction-service
  template:
    metadata:
      labels:
        app: swipe-transaction-service
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5196"
    spec:
      serviceAccountName: swipe-transaction-service
      securityContext:
        runAsNonRoot: false
        fsGroup: 1000
      containers:
      - name: swipe-transaction-service
        image: swipe-transaction-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 5196
          protocol: TCP
        envFrom:
        - configMapRef:
            name: swipe-transaction-service-config
        env:
        - name: ConnectionStrings__DefaultConnection
          valueFrom:
            secretKeyRef:
              name: swipe-transaction-service-secrets
              key: ConnectionStrings__DefaultConnection
        - name: Jwt__Secret
          valueFrom:
            secretKeyRef:
              name: swipe-transaction-service-secrets
              key: Jwt__Secret
        - name: RabbitMQ__Password
          valueFrom:
            secretKeyRef:
              name: swipe-transaction-service-secrets
              key: RabbitMQ__Password
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        startupProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: logs
          mountPath: /app/logs
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
      volumes:
      - name: config
        configMap:
          name: swipe-transaction-service-config
      - name: logs
        emptyDir: {}
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - swipe-transaction-service
              topologyKey: kubernetes.io/hostname
      tolerations:
      - key: "workload"
        operator: "Equal"
        value: "api"
        effect: "NoSchedule"
