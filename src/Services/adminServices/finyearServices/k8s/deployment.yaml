apiVersion: apps/v1
kind: Deployment
metadata:
  name: finyear-api
  namespace: finyear-api
  labels:
    app: finyear-api
    version: v1
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: finyear-api
  template:
    metadata:
      labels:
        app: finyear-api
        version: v1
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"
    spec:
      serviceAccountName: finyear-api
      securityContext:
        runAsNonRoot: false
        fsGroup: 1000
      containers:
      - name: finyear-api
        image: finyear-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 5000
          protocol: TCP
        - name: https
          containerPort: 5001
          protocol: TCP
        
        # Environment variables from ConfigMap
        envFrom:
        - configMapRef:
            name: finyear-api-config
        
        # Environment variables from Secret
        env:
        - name: ConnectionStrings__AdminDbConnection
          valueFrom:
            secretKeyRef:
              name: finyear-api-secrets
              key: ConnectionStrings__AdminDbConnection
        - name: Jwt__SecretKey
          valueFrom:
            secretKeyRef:
              name: finyear-api-secrets
              key: Jwt__SecretKey
        
        # Resource limits and requests
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        
        # Liveness probe
        livenessProbe:
          httpGet:
            path: /api/FinancialYear
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        # Readiness probe
        readinessProbe:
          httpGet:
            path: /api/FinancialYear
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 2
        
        # Startup probe (waits for app to start)
        startupProbe:
          httpGet:
            path: /api/FinancialYear
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 30
        
        # Volume mounts
        volumeMounts:
        - name: config
          mountPath: /app/config
          readOnly: true
        - name: logs
          mountPath: /app/logs
        
        # Security context for container
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          runAsNonRoot: false
          capabilities:
            drop:
            - ALL
      
      # Pod volumes
      volumes:
      - name: config
        configMap:
          name: finyear-api-config
      - name: logs
        emptyDir: {}
      
      # Pod affinity
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - finyear-api
              topologyKey: kubernetes.io/hostname
      
      # Tolerations for node taints
      tolerations:
      - key: "workload"
        operator: "Equal"
        value: "api"
        effect: "NoSchedule"
