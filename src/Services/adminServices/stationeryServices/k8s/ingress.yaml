apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stationery-service-ingress
  namespace: stationery-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - stationery-service.example.com
    secretName: stationery-service-tls
  rules:
  - host: stationery-service.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: stationery-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: stationery-service-network-policy
  namespace: stationery-service
spec:
  podSelector:
    matchLabels:
      app: stationery-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: stationery-service
    - podSelector:
        matchLabels:
          app: stationery-service-client
    ports:
    - protocol: TCP
      port: 5181
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5181
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: stationery-service
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
