apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scholarship-service-ingress
  namespace: scholarship-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - scholarship-service.example.com
    secretName: scholarship-service-tls
  rules:
  - host: scholarship-service.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: scholarship-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: scholarship-service-network-policy
  namespace: scholarship-service
spec:
  podSelector:
    matchLabels:
      app: scholarship-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: scholarship-service
    - podSelector:
        matchLabels:
          app: scholarship-service-client
    ports:
    - protocol: TCP
      port: 5166
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5166
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: scholarship-service
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
