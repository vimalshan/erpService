apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tds-service-ingress
  namespace: tds-service
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
  - hosts:
    - tds-service.example.com
    secretName: tds-service-tls
  rules:
  - host: tds-service.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tds-service
            port:
              number: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tds-service-network-policy
  namespace: tds-service
spec:
  podSelector:
    matchLabels:
      app: tds-service
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: lov-service
    - podSelector:
        matchLabels:
          app: tds-service-client
    ports:
    - protocol: TCP
      port: 5181
  - from:
    - namespaceSelector:
        matchLabels:
          name: ingress-nginx
    ports:
    - protocol: TCP
      port: 5181
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: lov-service
    ports:
    - protocol: TCP
      port: 1433
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 53
    - protocol: UDP
      port: 53
  - to:
    - podSelector: {}
    ports:
    - protocol: TCP
      port: 443
