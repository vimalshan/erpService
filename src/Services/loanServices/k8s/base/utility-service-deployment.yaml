---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: utility-service
  namespace: loan-services
  labels:
    app: utility-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: utility-service
  template:
    metadata:
      labels:
        app: utility-service
    spec:
      containers:
        - name: utility-service
          image: utility-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: loan-services-config
          env:
            - name: ConnectionStrings__DefaultConnection
              value: "Server=sqlserver,1433;Database=LOANDB;User Id=loan_app;Password=$(SQL_SA_PASSWORD);TrustServerCertificate=True;"
            - name: RabbitMQ__Host
              value: "rabbitmq"
            - name: RabbitMQ__Username
              valueFrom:
                secretKeyRef:
                  name: loan-services-secrets
                  key: RABBITMQ_USER
            - name: RabbitMQ__Password
              valueFrom:
                secretKeyRef:
                  name: loan-services-secrets
                  key: RABBITMQ_PASS
            - name: JwtSettings__Secret
              valueFrom:
                secretKeyRef:
                  name: loan-services-secrets
                  key: JWT_SECRET
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: utility-service
  namespace: loan-services
spec:
  selector:
    app: utility-service
  ports:
    - port: 8080
      targetPort: 8080
