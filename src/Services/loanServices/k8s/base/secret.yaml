apiVersion: v1
kind: Secret
metadata:
  name: loan-services-secrets
  namespace: loan-services
type: Opaque
# ---------------------------------------------------------------------------
# All values are base64-encoded.  Replace these BEFORE applying to production.
# Generate with: echo -n "yourvalue" | base64
#
#   SQL_SA_PASSWORD   (default: Loan@ERP#2025!)
#   RABBITMQ_USER     (default: loanadmin)
#   RABBITMQ_PASS     (default: LoanRabbit#2025!)
#   JWT_SECRET        (minimum 32 chars, change before go-live)
# ---------------------------------------------------------------------------
stringData:
  SQL_SA_PASSWORD:  "Loan@ERP#2025!"
  RABBITMQ_USER:    "loanadmin"
  RABBITMQ_PASS:    "LoanRabbit#2025!"
  JWT_SECRET:       "LoanERPJwtSecretKey2025!ChangeMe321"
