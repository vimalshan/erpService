# =============================================================================
# TEST PIPELINE — ddServices  (16 services)
#
# Purpose : Validate Build+Push (GHCR) → Verify for one module.
# Trigger : MANUAL ONLY — will never run automatically.
#
# Required pipeline variables (Azure DevOps → Pipeline → Edit → Variables):
#   GITHUB_TOKEN  – GitHub PAT with write:packages scope  (mark secret)
# =============================================================================

name: TEST · ddServices · $(Build.BuildId)

trigger: none
pr: none

parameters:
  - name: service
    displayName: 'Service to build (or "all")'
    type: string
    default: all
    values:
      - all
      - api-gateway
      - appraisal-service
      - authorization-service
      - compensation-service
      - competency-service
      - demand-management-service
      - document-service
      - employee-service
      - feedback-service
      - learning-service
      - objective-service
      - other-services
      - promotion-service
      - recruitment-service
      - reporting-service
      - transaction-service

variables:
  imagePrefix: 'ghcr.io/vimalshan/erp'
  GITHUB_ACTOR: 'vimalshan'

pool:
  vmImage: ubuntu-latest

stages:

  # ── BUILD + PUSH (single script — login, build, push per service) ───────────
  - stage: BuildAndPush
    displayName: 'Build & Push · ddServices'
    jobs:
      - job: BuildPushImages
        displayName: Build & Push Docker images
        steps:
          - checkout: self
            fetchDepth: 1

          - bash: |
              set -euo pipefail

              # ── Validate token ──
              if [ -z "${GITHUB_TOKEN:-}" ]; then
                echo "##[error]GITHUB_TOKEN is not set. Add it as a secret pipeline variable."
                exit 1
              fi
              TOKEN_LEN=${#GITHUB_TOKEN}
              echo "Token length: $TOKEN_LEN chars"
              if [ "$TOKEN_LEN" -lt 36 ]; then
                echo "##[error]GITHUB_TOKEN looks invalid ($TOKEN_LEN chars). A GitHub PAT is 40+ chars."
                exit 1
              fi

              # ── Login ──
              echo "Logging in as: $(GITHUB_ACTOR)"
              echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$(GITHUB_ACTOR)" --password-stdin
              echo "GHCR login succeeded."

              # ── Build + Push helper ──
              build_and_push() {
                local NAME=$1 CTX=$2 DF=$3
                if [ "${{{ parameters.service }}}" = "all" ] || [ "${{{ parameters.service }}}" = "$NAME" ]; then
                  echo ""
                  echo "======= Building $NAME ======="
                  docker build \
                    -t "$(imagePrefix)/$NAME:$(Build.BuildId)" \
                    -t "$(imagePrefix)/$NAME:latest" \
                    -f "$DF" "$CTX"

                  echo "======= Pushing $NAME ======="
                  docker push "$(imagePrefix)/$NAME:$(Build.BuildId)"
                  docker push "$(imagePrefix)/$NAME:latest"
                  echo "OK: $NAME built and pushed."
                fi
              }

              build_and_push 'api-gateway' 'src/Services/ddServices/apiGateway' 'src/Services/ddServices/apiGateway/Dockerfile'
              build_and_push 'appraisal-service' 'src/Services/ddServices/appraisalService' 'src/Services/ddServices/appraisalService/Dockerfile'
              build_and_push 'authorization-service' 'src/Services/ddServices/authorizationServices' 'src/Services/ddServices/authorizationServices/Dockerfile'
              build_and_push 'compensation-service' 'src/Services/ddServices/compensationServices' 'src/Services/ddServices/compensationServices/Dockerfile'
              build_and_push 'competency-service' 'src/Services/ddServices/competencyServices' 'src/Services/ddServices/competencyServices/Dockerfile'
              build_and_push 'demand-management-service' 'src/Services/ddServices/demandmanagementServices' 'src/Services/ddServices/demandmanagementServices/Dockerfile'
              build_and_push 'document-service' 'src/Services/ddServices/documentServices' 'src/Services/ddServices/documentServices/Dockerfile'
              build_and_push 'employee-service' 'src/Services/ddServices/employeeServices' 'src/Services/ddServices/employeeServices/Dockerfile'
              build_and_push 'feedback-service' 'src/Services/ddServices/feedbackServices' 'src/Services/ddServices/feedbackServices/Dockerfile'
              build_and_push 'learning-service' 'src/Services/ddServices/learningServices' 'src/Services/ddServices/learningServices/Dockerfile'
              build_and_push 'objective-service' 'src/Services/ddServices/objectiveServices' 'src/Services/ddServices/objectiveServices/Dockerfile'
              build_and_push 'other-services' 'src/Services/ddServices/OtherServices' 'src/Services/ddServices/OtherServices/Dockerfile'
              build_and_push 'promotion-service' 'src/Services/ddServices/promotionServices' 'src/Services/ddServices/promotionServices/Dockerfile'
              build_and_push 'recruitment-service' 'src/Services/ddServices/recruitmentServices' 'src/Services/ddServices/recruitmentServices/Dockerfile'
              build_and_push 'reporting-service' 'src/Services/ddServices/reportingServices' 'src/Services/ddServices/reportingServices/Dockerfile'
              build_and_push 'transaction-service' 'src/Services/ddServices/transactionServices' 'src/Services/ddServices/transactionServices/Dockerfile'

              echo ""
              echo "All done."
            displayName: 'Login, Build & Push all selected services'
            env:
              GITHUB_TOKEN: $(GITHUB_TOKEN)

  # ── VERIFY (separate agent — pulls from registry) ──────────────────────────
  - stage: Verify
    displayName: 'Verify · ddServices'
    dependsOn: BuildAndPush
    condition: succeeded()
    jobs:
      - job: VerifyImages
        displayName: Verify images exist in GHCR
        steps:
          - checkout: none

          - bash: |
              set -euo pipefail
              if [ -z "${GITHUB_TOKEN:-}" ]; then
                echo "##[error]GITHUB_TOKEN is not set."
                exit 1
              fi
              echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$(GITHUB_ACTOR)" --password-stdin
            displayName: Login to GHCR
            env:
              GITHUB_TOKEN: $(GITHUB_TOKEN)

          - script: |
              set -e
              verify_svc() {
                local NAME=$1
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  IMAGE="$(imagePrefix)/$NAME:$(Build.BuildId)"
                  echo "======= Verifying $IMAGE ======="
                  docker pull "$IMAGE"
                  echo "OK: $IMAGE"
                fi
              }
              verify_svc 'api-gateway'
              verify_svc 'appraisal-service'
              verify_svc 'authorization-service'
              verify_svc 'compensation-service'
              verify_svc 'competency-service'
              verify_svc 'demand-management-service'
              verify_svc 'document-service'
              verify_svc 'employee-service'
              verify_svc 'feedback-service'
              verify_svc 'learning-service'
              verify_svc 'objective-service'
              verify_svc 'other-services'
              verify_svc 'promotion-service'
              verify_svc 'recruitment-service'
              verify_svc 'reporting-service'
              verify_svc 'transaction-service'
            displayName: 'Pull & verify all selected images'
