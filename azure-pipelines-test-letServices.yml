# =============================================================================
# TEST PIPELINE — letServices  (8 services)
#
# Purpose : Validate Build+Push (GHCR) → Verify for one module.
# Trigger : MANUAL ONLY — will never run automatically.
#
# Required pipeline variables (Azure DevOps → Pipeline → Edit → Variables):
#   GITHUB_TOKEN  – GitHub PAT with write:packages scope  (mark secret)
# =============================================================================

name: TEST · letServices · $(Build.BuildId)

trigger: none
pr: none

parameters:
  - name: service
    displayName: 'Service to build (or "all")'
    type: string
    default: all
    values:
      - all
      - api-gateway
      - course-service
      - development-service
      - leave-service
      - let-transaction-service
      - master-service
      - request-service
      - review-service

variables:
  imagePrefix: 'ghcr.io/vimalshan/erp'
  GITHUB_ACTOR: 'vimalshan'

pool:
  vmImage: ubuntu-latest

stages:

  # ── BUILD + PUSH (same agent — images stay in local docker) ────────────────
  - stage: BuildAndPush
    displayName: 'Build & Push · letServices'
    jobs:
      - job: BuildPushImages
        displayName: Build & Push Docker images
        steps:
          - checkout: self
            fetchDepth: 1

          - bash: |
              set -euo pipefail
              if [ -z "${GITHUB_TOKEN:-}" ]; then
                echo "##[error]GITHUB_TOKEN is not set. Add it as a secret pipeline variable."
                echo "##[error]Go to: Pipeline → Edit → Variables → New variable"
                echo "##[error]  Name: GITHUB_TOKEN"
                echo "##[error]  Value: <your GitHub PAT with write:packages scope>"
                echo "##[error]  Check 'Keep this value secret'"
                exit 1
              fi
              TOKEN_LEN=${#GITHUB_TOKEN}
              echo "Token length: $TOKEN_LEN chars"
              if [ "$TOKEN_LEN" -lt 36 ]; then
                echo "##[error]GITHUB_TOKEN looks invalid ($TOKEN_LEN chars). A GitHub PAT is 40+ chars."
                echo "##[error]Please regenerate: GitHub → Settings → Developer settings → Personal access tokens"
                echo "##[error]Required scopes: write:packages, read:packages"
                exit 1
              fi
              echo "Logging in as: $(GITHUB_ACTOR)"
              echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$(GITHUB_ACTOR)" --password-stdin
              echo "GHCR login succeeded."
            displayName: Login to GHCR
            env:
              GITHUB_TOKEN: $(GITHUB_TOKEN)

          - script: |
              set -e
              build_svc() {
                local NAME=$1 CTX=$2 DF=$3
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  echo "======= Building $NAME ======="
                  docker build \
                    -t "$(imagePrefix)/$NAME:$(Build.BuildId)" \
                    -t "$(imagePrefix)/$NAME:latest" \
                    -f "$DF" "$CTX"
                fi
              }
              build_svc 'api-gateway' 'src/Services/letServices/apiGateway' 'src/Services/letServices/apiGateway/Dockerfile'
              build_svc 'course-service' 'src/Services/letServices/courseServices' 'src/Services/letServices/courseServices/Dockerfile'
              build_svc 'development-service' 'src/Services/letServices/developmentServices' 'src/Services/letServices/developmentServices/Dockerfile'
              build_svc 'leave-service' 'src/Services/letServices/leaveServices' 'src/Services/letServices/leaveServices/Dockerfile'
              build_svc 'let-transaction-service' 'src/Services/letServices/letTransactionServices' 'src/Services/letServices/letTransactionServices/Dockerfile'
              build_svc 'master-service' 'src/Services/letServices/masterServices' 'src/Services/letServices/masterServices/Dockerfile'
              build_svc 'request-service' 'src/Services/letServices/requestServices' 'src/Services/letServices/requestServices/Dockerfile'
              build_svc 'review-service' 'src/Services/letServices/reviewServices' 'src/Services/letServices/reviewServices/Dockerfile'
            displayName: 'docker build all selected services'

          - script: |
              set -e
              push_svc() {
                local NAME=$1
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  echo "======= Pushing $NAME ======="
                  docker push "$(imagePrefix)/$NAME:$(Build.BuildId)"
                  docker push "$(imagePrefix)/$NAME:latest"
                fi
              }
              push_svc 'api-gateway'
              push_svc 'course-service'
              push_svc 'development-service'
              push_svc 'leave-service'
              push_svc 'let-transaction-service'
              push_svc 'master-service'
              push_svc 'request-service'
              push_svc 'review-service'
            displayName: 'docker push all selected services'

  # ── VERIFY (separate agent — pulls from registry) ──────────────────────────
  - stage: Verify
    displayName: 'Verify · letServices'
    dependsOn: BuildAndPush
    condition: succeeded()
    jobs:
      - job: VerifyImages
        displayName: Verify images exist in GHCR
        steps:
          - checkout: none

          - bash: |
              set -euo pipefail
              if [ -z "${GITHUB_TOKEN:-}" ]; then
                echo "##[error]GITHUB_TOKEN is not set."
                exit 1
              fi
              echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$(GITHUB_ACTOR)" --password-stdin
            displayName: Login to GHCR
            env:
              GITHUB_TOKEN: $(GITHUB_TOKEN)

          - script: |
              set -e
              verify_svc() {
                local NAME=$1
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  IMAGE="$(imagePrefix)/$NAME:$(Build.BuildId)"
                  echo "======= Verifying $IMAGE ======="
                  docker pull "$IMAGE"
                  echo "OK: $IMAGE"
                fi
              }
              verify_svc 'api-gateway'
              verify_svc 'course-service'
              verify_svc 'development-service'
              verify_svc 'leave-service'
              verify_svc 'let-transaction-service'
              verify_svc 'master-service'
              verify_svc 'request-service'
              verify_svc 'review-service'
            displayName: 'Pull & verify all selected images'
