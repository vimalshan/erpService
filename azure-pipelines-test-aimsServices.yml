# =============================================================================
# TEST PIPELINE — aimsServices  (11 services)
#
# Purpose : Validate Build+Push (GHCR) → Verify for one module.
# Trigger : MANUAL ONLY — will never run automatically.
#
# Required pipeline variables (Azure DevOps → Pipeline → Edit → Variables):
#   GITHUB_TOKEN  – GitHub PAT with write:packages scope  (mark secret)
# =============================================================================

name: TEST · aimsServices · $(Build.BuildId)

trigger: none
pr: none

parameters:
  - name: service
    displayName: 'Service to build (or "all")'
    type: string
    default: all
    values:
      - all
      - api-gateway
      - access-service
      - aims-transaction-service
      - attendance-service
      - bus-services
      - calendar-service
      - employee-service
      - groupincentive-service
      - leave-services
      - reference-service
      - visitor-services

variables:
  imagePrefix: 'ghcr.io/vimalshan/erp'
  GITHUB_ACTOR: 'vimalshan'

pool:
  vmImage: ubuntu-latest

stages:

  # ── BUILD + PUSH (same agent — images stay in local docker) ────────────────
  - stage: BuildAndPush
    displayName: 'Build & Push · aimsServices'
    jobs:
      - job: BuildPushImages
        displayName: Build & Push Docker images
        steps:
          - checkout: self
            fetchDepth: 1

          - bash: |
              set -euo pipefail
              if [ -z "${GITHUB_TOKEN:-}" ]; then
                echo "##[error]GITHUB_TOKEN is not set. Add it as a secret pipeline variable."
                echo "##[error]Go to: Pipeline → Edit → Variables → New variable"
                echo "##[error]  Name: GITHUB_TOKEN"
                echo "##[error]  Value: <your GitHub PAT with write:packages scope>"
                echo "##[error]  Check 'Keep this value secret'"
                exit 1
              fi
              TOKEN_LEN=${#GITHUB_TOKEN}
              echo "Token length: $TOKEN_LEN chars"
              if [ "$TOKEN_LEN" -lt 36 ]; then
                echo "##[error]GITHUB_TOKEN looks invalid ($TOKEN_LEN chars). A GitHub PAT is 40+ chars."
                echo "##[error]Please regenerate: GitHub → Settings → Developer settings → Personal access tokens"
                echo "##[error]Required scopes: write:packages, read:packages"
                exit 1
              fi
              echo "Logging in as: $(GITHUB_ACTOR)"
              echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$(GITHUB_ACTOR)" --password-stdin
              echo "GHCR login succeeded."
            displayName: Login to GHCR
            env:
              GITHUB_TOKEN: $(GITHUB_TOKEN)

          - script: |
              set -e
              build_svc() {
                local NAME=$1 CTX=$2 DF=$3
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  echo "======= Building $NAME ======="
                  docker build \
                    -t "$(imagePrefix)/$NAME:$(Build.BuildId)" \
                    -t "$(imagePrefix)/$NAME:latest" \
                    -f "$DF" "$CTX"
                fi
              }
              build_svc 'api-gateway' 'src/Services/aimsServices/ApiGateway' 'src/Services/aimsServices/ApiGateway/Dockerfile'
              build_svc 'access-service' 'src/Services/aimsServices/accessServices' 'src/Services/aimsServices/accessServices/Dockerfile'
              build_svc 'aims-transaction-service' 'src/Services/aimsServices/aimsTransactionServices' 'src/Services/aimsServices/aimsTransactionServices/Dockerfile'
              build_svc 'attendance-service' 'src/Services/aimsServices/attendanceServices' 'src/Services/aimsServices/attendanceServices/Dockerfile'
              build_svc 'bus-services' 'src/Services/aimsServices/busServices' 'src/Services/aimsServices/busServices/Dockerfile'
              build_svc 'calendar-service' 'src/Services/aimsServices/calendarServices' 'src/Services/aimsServices/calendarServices/Dockerfile'
              build_svc 'employee-service' 'src/Services/aimsServices/employeeServices' 'src/Services/aimsServices/employeeServices/Dockerfile'
              build_svc 'groupincentive-service' 'src/Services/aimsServices/groupincentiveServices' 'src/Services/aimsServices/groupincentiveServices/Dockerfile'
              build_svc 'leave-services' 'src/Services/aimsServices/leaveServices' 'src/Services/aimsServices/leaveServices/Dockerfile'
              build_svc 'reference-service' 'src/Services/aimsServices/referenceServices' 'src/Services/aimsServices/referenceServices/Dockerfile'
              build_svc 'visitor-services' 'src/Services/aimsServices/visitorServices' 'src/Services/aimsServices/visitorServices/Dockerfile'
            displayName: 'docker build all selected services'

          - script: |
              set -e
              push_svc() {
                local NAME=$1
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  echo "======= Pushing $NAME ======="
                  docker push "$(imagePrefix)/$NAME:$(Build.BuildId)"
                  docker push "$(imagePrefix)/$NAME:latest"
                fi
              }
              push_svc 'api-gateway'
              push_svc 'access-service'
              push_svc 'aims-transaction-service'
              push_svc 'attendance-service'
              push_svc 'bus-services'
              push_svc 'calendar-service'
              push_svc 'employee-service'
              push_svc 'groupincentive-service'
              push_svc 'leave-services'
              push_svc 'reference-service'
              push_svc 'visitor-services'
            displayName: 'docker push all selected services'

  # ── VERIFY (separate agent — pulls from registry) ──────────────────────────
  - stage: Verify
    displayName: 'Verify · aimsServices'
    dependsOn: BuildAndPush
    condition: succeeded()
    jobs:
      - job: VerifyImages
        displayName: Verify images exist in GHCR
        steps:
          - checkout: none

          - bash: |
              set -euo pipefail
              if [ -z "${GITHUB_TOKEN:-}" ]; then
                echo "##[error]GITHUB_TOKEN is not set."
                exit 1
              fi
              echo "$GITHUB_TOKEN" | docker login ghcr.io -u "$(GITHUB_ACTOR)" --password-stdin
            displayName: Login to GHCR
            env:
              GITHUB_TOKEN: $(GITHUB_TOKEN)

          - script: |
              set -e
              verify_svc() {
                local NAME=$1
                if [ "${{ parameters.service }}" = "all" ] || [ "${{ parameters.service }}" = "$NAME" ]; then
                  IMAGE="$(imagePrefix)/$NAME:$(Build.BuildId)"
                  echo "======= Verifying $IMAGE ======="
                  docker pull "$IMAGE"
                  echo "OK: $IMAGE"
                fi
              }
              verify_svc 'api-gateway'
              verify_svc 'access-service'
              verify_svc 'aims-transaction-service'
              verify_svc 'attendance-service'
              verify_svc 'bus-services'
              verify_svc 'calendar-service'
              verify_svc 'employee-service'
              verify_svc 'groupincentive-service'
              verify_svc 'leave-services'
              verify_svc 'reference-service'
              verify_svc 'visitor-services'
            displayName: 'Pull & verify all selected images'
